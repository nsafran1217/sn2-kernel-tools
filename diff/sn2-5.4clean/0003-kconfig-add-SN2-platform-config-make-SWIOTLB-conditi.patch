From 94fb67bef3c15ead65661e39fb3d4a812eb5617b Mon Sep 17 00:00:00 2001
From: SN2 Dev <sn2@dev>
Date: Sun, 8 Mar 2026 02:08:43 +0000
Subject: [PATCH 3/6] kconfig: add SN2 platform config, make SWIOTLB
 conditional

---
 arch/ia64/Kconfig  | 24 +++++++++++++++++++++---
 arch/ia64/Makefile |  1 +
 2 files changed, 22 insertions(+), 3 deletions(-)

diff --git a/arch/ia64/Kconfig b/arch/ia64/Kconfig
index 1671447..5b52c10 100644
--- a/arch/ia64/Kconfig
+++ b/arch/ia64/Kconfig
@@ -33,8 +33,8 @@ config IA64
 	select HAVE_ARCH_TRACEHOOK
 	select HAVE_MEMBLOCK_NODE_MAP
 	select HAVE_VIRT_CPU_ACCOUNTING
-	select ARCH_HAS_DMA_COHERENT_TO_PFN
-	select ARCH_HAS_SYNC_DMA_FOR_CPU
+	select ARCH_HAS_DMA_COHERENT_TO_PFN if SWIOTLB
+	select ARCH_HAS_SYNC_DMA_FOR_CPU if SWIOTLB
 	select VIRT_TO_BUS
 	select GENERIC_IRQ_PROBE
 	select GENERIC_PENDING_IRQ if SMP
@@ -48,7 +48,7 @@ config IA64
 	select ARCH_THREAD_STACK_ALLOCATOR
 	select ARCH_CLOCKSOURCE_DATA
 	select GENERIC_TIME_VSYSCALL
-	select SWIOTLB
+	select SWIOTLB if !IA64_SGI_SN2
 	select SYSCTL_ARCH_UNALIGN_NO_WARN
 	select HAVE_MOD_ARCH_SPECIFIC
 	select MODULES_USE_ELF_RELA
@@ -71,6 +71,7 @@ config 64BIT
 
 config ZONE_DMA32
 	def_bool y
+	depends on !IA64_SGI_SN2
 
 config MMU
 	bool
@@ -184,6 +185,21 @@ config IA64_L1_CACHE_SHIFT
 	default "7" if MCKINLEY
 	default "6" if ITANIUM
 
+config IA64_SGI_SN2
+	bool "SGI-SN2 (Altix) support"
+	select NUMA
+	select ACPI_NUMA
+	select IA64_UNCACHED_ALLOCATOR
+	help
+	  Selecting this option will add support for SGI Altix (SN2) systems.
+	  This builds a kernel with SN2 platform code including custom I/O,
+	  DMA, IRQ, and NUMA support for the SHUB interconnect.  If you have
+	  an SGI Altix system, say Y.  Otherwise, say N.
+
+config SGI_SN
+	def_bool y
+	depends on IA64_SGI_SN2
+
 config IA64_SGI_UV
 	bool "SGI-UV support"
 	help
@@ -410,6 +426,8 @@ config IA64_HP_AML_NFW
 	  the "force" module parameter, e.g., with the "aml_nfw.force"
 	  kernel command line option.
 
+source "drivers/sn/Kconfig"
+
 config KEXEC
 	bool "kexec system call"
 	depends on !SMP || HOTPLUG_CPU
diff --git a/arch/ia64/Makefile b/arch/ia64/Makefile
index 3224000..be7f14f 100644
--- a/arch/ia64/Makefile
+++ b/arch/ia64/Makefile
@@ -50,6 +50,7 @@ head-y := arch/ia64/kernel/head.o
 
 libs-y				+= arch/ia64/lib/
 core-y				+= arch/ia64/kernel/ arch/ia64/mm/
+core-$(CONFIG_IA64_SGI_SN2)	+= arch/ia64/sn/
 core-$(CONFIG_IA64_SGI_UV)	+= arch/ia64/uv/
 
 drivers-y			+= arch/ia64/pci/ arch/ia64/hp/common/
-- 
2.43.0

